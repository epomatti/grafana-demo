---
- name: Install dependencies and utilities
  ansible.builtin.apt:
    pkg:
      # Required by Alloy
      - gcc      
      # May be used when running the Alloy installation script for Linux
      - unzip
      - zip
